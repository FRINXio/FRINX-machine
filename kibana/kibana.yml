# Custom Kibana configuration for docker target
server.name: kibana
server.host: ${NODE_HOST}
elasticsearch.hosts: [ "http://elasticsearch:9200" ]
xpack.monitoring.ui.container.elasticsearch.enabled: true
# console.enabled: false
