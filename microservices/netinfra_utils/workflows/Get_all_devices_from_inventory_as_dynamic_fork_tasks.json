{
  "name": "Get_all_devices_from_inventory_as_dynamic_fork_tasks",
  "description": "get all devices in inventory database as dynamic fork tasks - BASICS,INVENTORY",
  "version": 1,
  "inputParameters": [
    "labels[Enter one or multiple labels to select devices. Match type is ALL.]",
    "task[Name of a task to schedule for subsequent execution in the output #tasks]",
    "optional[When set to true - tasks continues even if the task fails]"
  ],
  "tasks": [
    {
      "name": "INVENTORY_get_all_devices_as_dynamic_fork_tasks",
      "taskReferenceName": "INVENTORY_get_all_devices_as_dynamic_fork_tasks",
      "type": "SIMPLE",
      "inputParameters": {
        "labels": "${workflow.input.labels}",
        "task": "${workflow.input.task}",
        "task_params": "${workflow.input.task_params}",
        "optional": "${workflow.input.optional}"
      }
    }
  ],
  "outputParameters": {
    "dynamic_tasks_i": "${INVENTORY_get_all_devices_as_dynamic_fork_tasks.output.dynamic_tasks_i}",
    "dynamic_tasks": "${INVENTORY_get_all_devices_as_dynamic_fork_tasks.output.dynamic_tasks}"
  },
  "schemaVersion": 2
}