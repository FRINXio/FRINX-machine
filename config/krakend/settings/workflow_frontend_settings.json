{
    "endpoints": [
        {
            "endpoint": "/",
            "method": "GET",
            "url_pattern": "/"
        },
        {
            "endpoint": "/static/{a}",
            "method": "GET",
            "url_pattern": "/static/{a}"
        },
        {
            "endpoint": "/uniflow/{a}",
            "method": "GET",
            "url_pattern": "/uniflow/{a}"
        },
        {
            "endpoint": "/uniflow/{a}/{b}",
            "method": "GET",
            "url_pattern": "/uniflow/{a}/{b}"
        }
    ]
}